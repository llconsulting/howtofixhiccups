# Host video

True-motion UGC, when it lands, replaces the files in `/assets/video/` under the same names. This folder stays empty so unused paths do not 404.

Stills stay the host. Start does not wait on video.
